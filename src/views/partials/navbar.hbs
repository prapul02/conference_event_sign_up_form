<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Home</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav"
    aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item {{#if (ifEquality title "signup")}}active{{/if}}">
        <a class="nav-link" href="/signup">Sign up {{#if (ifEquality title "signup")}}<span
            class="sr-only">(current)</span>{{/if}}</a>
      </li>
      <li class="nav-item {{#if (ifEquality title "analytics")}}active{{/if}}">
        <a class="nav-link" href="/analytics">Analytics {{#if (ifEquality title "analytics")}}<span
            class="sr-only">(current)</span>{{/if}}</a>
      </li>
    </ul>
  </div>
</nav>